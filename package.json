{
  "name": "obsidian-momentum",
  "version": "0.1.1",
  "description": "Momentum — all-in-one life dashboard for Obsidian (habits, tasks, fitness, nutrition, studies) stored as plain markdown.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "keywords": ["obsidian-plugin"],
  "author": "Jaime Nagase",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "0.20.0",
    "obsidian": "latest",
    "tslib": "^2.6.2",
    "typescript": "^5.4.0"
  }
}
